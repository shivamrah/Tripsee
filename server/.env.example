# Example environment variables for TripSee server
# Copy this file to .env and fill in real values (DO NOT commit secrets)

# MongoDB connection string (include DB name). Example:
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.lptvimk.mongodb.net/tripseeDB?retryWrites=true&w=majority
MONGO_URI=

# JSON Web Token secret used to sign auth tokens. Use a long random string.
# Example: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# Optional: server port
PORT=5000
# Example `.env` for the server. Keep secrets out of source control.
# Replace <PASSWORD> with your actual DB password (URL-encode if it contains special chars).
MONGO_URI=mongodb+srv://malavathakash1234_db_user:<PASSWORD>@cluster0.lptvimk.mongodb.net/?appName=Cluster0
